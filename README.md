## 🔐 ระบบเข้าสู่ระบบ (User Login)

ระบบมีผู้ใช้งานทั้งหมด 3 ระดับ ได้แก่ Admin, Tech และ User

### 👤 บัญชีทดสอบ (Test Account)

| ระดับผู้ใช้ | Username | Password |
|------------|----------|----------|
| Admin | admin | admin123 |
| Tech | tech | tech123 |
| User | user | user123 |

---

## ▶️ วิธีเข้าสู่ระบบ

1. เปิดหน้า Login ของระบบ
2. กรอก Username และ Password
3. กดปุ่ม Login
4. ระบบจะพาเข้าสู่หน้าตามสิทธิ์ผู้ใช้

---

## 📌 สิทธิ์การใช้งานแต่ละระดับ

### 🛡️ Admin
- จัดการผู้ใช้
- จัดการฐานข้อมูล
- ดูรายงานทั้งหมด
- เพิ่ม / ลบ / แก้ไขข้อมูล

### 🛠️ Tech
- แก้ไขข้อมูลระบบ
- ตรวจสอบปัญหา
- จัดการข้อมูลบางส่วน

### 👤 User
- ดูข้อมูลของตนเอง
- ใช้งานระบบพื้นฐาน
- แก้ไขโปรไฟล์


<<<<<< --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
>>>>>>

# 📌 การเชื่อมต่อ MySQL บน Railway ด้วย MySQL Workbench

เอกสารนี้อธิบายขั้นตอนการเข้าใช้งานฐานข้อมูล MySQL ที่อยู่บน Railway ผ่านโปรแกรม MySQL Workbench

---

## ✅ สิ่งที่ต้องมี
- ติดตั้งโปรแกรม MySQL Workbench
- มีบัญชี Railway และสร้าง MySQL Service แล้ว

---

## 🔑 ข้อมูลสำหรับเชื่อมต่อ (ดูจาก Railway)

เข้าไปที่หน้า Project → MySQL Service → Variables  
แล้วดูค่าต่อไปนี้:

| รายการ | ตัวแปรใน Railway |
|--------|------------------|
| Host   | MYSQL_PUBLIC_URL |
| Port   | MYSQLPORT |
| User   | MYSQLUSER |
| Pass   | MYSQLPASSWORD |
| DB     | MYSQLDATABASE |

---

## ⚙️ ขั้นตอนการตั้งค่าใน MySQL Workbench

1. เปิดโปรแกรม MySQL Workbench
2. กดปุ่ม `+` ที่ MySQL Connections
3. กรอกข้อมูลดังนี้

### Connection Settings

| ช่อง | ค่า |
|------|-----|
| Connection Name | railway-public |
| Hostname | switchback.proxy.rlwy.net |
| Port | 28852 |
| Username | root |
| Password | รหัสจาก MYSQLPASSWORD |

> หมายเหตุ: ค่า Host และ Port ดูจาก MYSQL_PUBLIC_URL

---

## 🔐 การใส่รหัสผ่าน

1. กดปุ่ม `Store in Keychain / Store in Vault`
2. วางรหัสจาก MYSQLPASSWORD
3. กด OK

---

## ▶️ การเชื่อมต่อ

กดปุ่ม `Test Connection`  
ถ้าผ่าน → กด OK → ดับเบิลคลิกเพื่อเข้าใช้งาน

---

## 📊 วิธีดูตารางในฐานข้อมูล

### 1. รีเฟรช Schema
คลิกขวาที่ `Schemas` → Refresh All

### 2. เปิดดูตาราง
Schemas
└── railway
└── Tables

### 3. ดูข้อมูลในตาราง
คลิกขวาที่ชื่อตาราง  
เลือก `Select Rows - Limit 1000`

---

## 💻 คำสั่ง SQL พื้นฐาน

```sql
-- เลือกฐานข้อมูล
USE railway;

-- ดูตารางทั้งหมด
SHOW TABLES;

-- ดูข้อมูลในตาราง
SELECT * FROM users;